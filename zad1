#include <stdio.h>

main()
{
	char a[6];

	for (int i = 0; i < 6; i++)
	{
		printf("Unesi %d. znak u polje: ", i + 1);
		scanf(" %c", a[i]);
	}

	for (int i = 0; i < 6; i++)
		if ('0' <= a[i] && a[i] <= '9')
			printf("%c\n", a[i]);
}
